[project]
name = "ai-service"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pypdf>=3.0.0",
    "python-docx>=0.9.0",
]
[tool.fastapi]
entrypoint = "app.main:app"
